# Sort ZEN 출시 매뉴얼

> 코드 작업 완료 후 사람이 직접 수행해야 하는 단계별 가이드

---

## 1. Play Console 상품 등록

### 1-1. 구독 상품 (2종)

1. [Play Console](https://play.google.com/console) → 앱 선택 → 수익 창출 → 구독
2. 아래 2개 구독 생성:

| 상품 ID | 종류 | 가격 |
|---------|------|------|
| `sortzen_remove_ads_monthly` | 자동 갱신 구독 | ₩2,500/월 |
| `sortzen_remove_ads_yearly` | 자동 갱신 구독 | ₩19,900/년 |

3. 각 구독에 base plan + offer 설정
4. 자동 환율 변환 활성화

### 1-2. 인앱 상품 (1종)

1. 수익 창출 → 인앱 상품
2. 아래 상품 생성:

| 상품 ID | 종류 | 가격 |
|---------|------|------|
| `sortzen_remove_ads_lifetime` | 비소모성 (관리되는 상품) | ₩9,900 |

### 1-3. 테스터 설정

1. 설정 → 라이선스 테스트 → 본인 Gmail 추가
2. 테스트 → 내부 테스트 → 테스터 목록에 동일 Gmail 추가

---

## 2. EAS Build

### 2-1. 사전 준비

```bash
# EAS CLI 설치 (없으면)
npm install -g eas-cli

# 로그인
eas login

# 프로젝트 초기화 (처음 한 번)
eas build:configure
```

### 2-2. 프리뷰 빌드 (테스트용 APK)

```bash
npx eas build --platform android --profile preview
```

### 2-3. 프로덕션 빌드 (AAB)

```bash
npx eas build --platform android --profile production
```

빌드 완료 후 `.aab` 파일을 Play Console에 업로드한다.

---

## 3. 실기기 QA 체크리스트

빌드가 나오면 아래 항목을 순서대로 확인한다.

### 기능

- [ ] 앱 첫 실행 → 온보딩 3컷 표시
- [ ] 온보딩 완료 → 메인메뉴 진입
- [ ] 두 번째 실행 → 온보딩 안 뜸
- [ ] 클래식 모드 진입 → 레벨 1 시작
- [ ] 튜브 탭 → 선택 사운드 + 진동
- [ ] pour 성공 → 실로폰 사운드 + 진동
- [ ] 레벨 클리어 → 별 3개 모달 + 코인 +10
- [ ] ZEN 모드 → BGM 재생 + 끝없이 새 퍼즐
- [ ] 되돌리기/리셋 버튼 동작
- [ ] 설정 → 효과음/BGM/진동 각각 on/off
- [ ] 설정 → 언어 ko ↔ en 전환 → 모든 텍스트 변경
- [ ] 설정 → 테마 pastel 선택 가능, neon/dark 잠금 표시
- [ ] 통계 화면 → 레벨/클리어 수/플레이 시간/코인 표시

### 광고

- [ ] 메인메뉴 하단 배너 노출
- [ ] 설정/상점/통계 하단 배너 노출
- [ ] 게임 화면에는 배너 없음
- [ ] 첫 5분간 전면 광고 안 뜸
- [ ] 클래식 3레벨 클리어 후 전면 광고 노출
- [ ] 60초 안에 다시 클리어 → 전면 광고 안 뜸
- [ ] ZEN 모드 클리어 → 전면 광고 절대 안 뜸
- [ ] GDPR 동의 폼 노출 (EU 시뮬레이션)

### 결제 (라이센스 테스터 계정으로)

- [ ] 상점 → 평생 구매 → `isPremium = true`
- [ ] 상점 → 월간/연간 구독 → `isPremium = true`
- [ ] 구독 후 배너/전면 광고 즉시 사라짐
- [ ] 구독 후 neon/dark 테마 잠금 해제
- [ ] 앱 종료 → 재시작 → 프리미엄 상태 유지 (자동 복원)
- [ ] "이전 구매 복원" 버튼 동작
- [ ] 결제 실패 시 에러 메시지 표시

---

## 4. 스토어 등록 자산

### 4-1. 필수 자산

| 자산 | 규격 | 상태 |
|------|------|------|
| 앱 아이콘 | 512 x 512 PNG | `assets/icon.png` (placeholder → 교체 필요) |
| 피처 그래픽 | 1024 x 500 PNG | 미생성 |
| 스크린샷 | 최소 2장 (권장 5장) | 미생성 |

### 4-2. 스크린샷 추천 구성 (5장)

1. 메인 메뉴 (앱 이름 + 모드 선택)
2. 클래식 모드 게임 플레이
3. ZEN 모드 게임 플레이
4. 클리어 모달 (별 3개)
5. 설정 화면 (테마 3종)

### 4-3. 아이콘/그래픽 제작 팁

- Canva, Figma 등으로 제작 가능
- 앱 아이콘: 튜브 + 액체 + 파스텔 배경
- 피처 그래픽: "Sort ZEN" 타이틀 + 게임 스크린샷 합성

---

## 5. Play Console 설정

### 5-1. 스토어 등록정보

- [ ] 앱 이름: `Sort ZEN - 정렬 퍼즐`
- [ ] 간단한 설명 (80자): `편안한 ASMR 정렬 퍼즐. 광고 없는 게임 플레이, 무한 레벨.`
- [ ] 자세한 설명 (4000자 이내): 주요 기능 나열
- [ ] 아이콘 / 피처 그래픽 / 스크린샷 업로드
- [ ] 카테고리: 퍼즐
- [ ] 태그: puzzle, sorting, relaxing, ASMR, casual

### 5-2. 콘텐츠 등급

- [ ] 등급 질문지 작성 → 만 3세 이상 (PEGI 3 / Everyone)

### 5-3. 개인정보처리방침

- [ ] 개인정보처리방침 URL 준비 (GitHub Pages, Notion 등)
- [ ] Play Console에 URL 등록

### 5-4. 데이터 안전 섹션

- [ ] 수집 데이터: 광고 ID (AdMob), 구매 내역 (IAP)
- [ ] 데이터 공유: 광고 파트너 (Google AdMob)
- [ ] 암호화: 전송 중 암호화 (HTTPS)
- [ ] 데이터 삭제 요청: 앱 삭제 시 로컬 데이터 삭제

### 5-5. 광고 포함 여부

- [ ] "예, 광고가 포함되어 있습니다" 체크

---

## 6. 출시 순서

```
1. Play Console 상품 등록 (구독 2종 + 평생 1종)
2. EAS 프리뷰 빌드 → 실기기 QA
3. QA 통과 후 스토어 자산 준비
4. EAS 프로덕션 빌드 (.aab)
5. Play Console → 내부 테스트 트랙 업로드
6. 내부 테스트로 결제 흐름 최종 확인
7. 프로덕션 트랙으로 승격
8. 검토 대기 (보통 1~3일)
9. 출시 완료
```

---

## 7. 출시 후 모니터링

| 항목 | 확인 위치 | 목표 |
|------|----------|------|
| 크래시 | Play Console → Android vitals | ANR < 0.5% |
| 광고 수익 | AdMob 대시보드 | ARPDAU $0.05~0.15 |
| 구독 전환 | Play Console → 구독 | 1%+ |
| 평점 | Play Store | 4.5+ |
| D1 리텐션 | Firebase Analytics | 35%+ |
